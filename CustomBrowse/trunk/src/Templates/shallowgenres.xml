<?xml version="1.0" encoding="utf-8"?>
<custombrowse>
	<template>
		<name>Genre Albums</name>
		<description>Genres menu that works the same way as the standard slimserver albums menu but is a bit more shallow by moving directly from genres to albums instead of requesting the user to select artists before albums</description>
		<parameter type="text" id="menuname" name="Menu name" value="Genre Albums"/>
		<parameter type="text" id="menuorder" name="Menu sort order (1-100)" value="50"/>
		<parameter type="checkbox" id="playalltracks" name="Play all songs in selected album" value="1"/>
		<parameter type="text" id="includedclients" name="Only include on these players (empty = all)" value=""/>
		<parameter type="text" id="excludedclients" name="Exclude from these players" value=""/>
	</template>
</custombrowse>

